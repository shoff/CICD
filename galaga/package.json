{
  "name": "galaga-clone",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A Galaga-style arcade shooter for the browser, written in TypeScript.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
